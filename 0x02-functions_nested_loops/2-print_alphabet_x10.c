#include "main.h"

/**
  * print_alphabet_x10 - Make alphabet x10 times
  *
  * Return: void
  */
void print_alphabet_x10(void)
{
	char alphabet;
	int numerOfTimes = 0;

	while (numerOfTimes <= 9)
	{
		for (alphabet; = 'a'; alphabet; <= 'z'; alphabet;++)
		{
			_putchar(alphabet;);
		}

		_putchar('\n');

		numerOfTimes++;
	}
}
