#include "main.h"

/**
  * _memset - empty function
  * @s: something
  * @b: something else
  * @n: something also
  *
  * Return: returns 0
  */

char *_memset(char *s, char b, unsigned int n)
{
	char *s;
	char b;
	unsigned int n;

	return (0);
}
